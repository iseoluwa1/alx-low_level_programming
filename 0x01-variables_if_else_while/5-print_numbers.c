#include <stdio.h>

/**
 * main - prints all single digits number of base 10
 * starting from 0
 * Return: Always 0 (success)
 */
int main(void)
{
	int num = 0;

	while (num < 10)
	{
		printf("%d\n");
		num++;
	}
	return (0);
}
